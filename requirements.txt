# --- Core ---
httpx>=0.27,<1.0
python-dotenv>=1.0
pydantic>=2.6,<3.0
pyyaml>=6.0
typer>=0.12
rich>=13.7

# --- Scheduling ---
APScheduler>=3.10,<4.0

# --- Polymarket ---
py-clob-client>=0.34          # офіційний SDK
websockets>=12.0              # для CLOB ws-стріму

# --- Crypto data ---
# Прямі HTTP-запити до CoinGecko + Binance — без важких SDK

# --- English bot / Grok ---
openai>=1.40                  # Grok API сумісний з OpenAI SDK (base_url = https://api.x.ai/v1)

# --- Notifications ---
# Telegram через простий HTTP-запит, без python-telegram-bot

# --- Dev / tests ---
pytest>=8.0
pytest-asyncio>=0.23
